```markdown
# MinNPU: MiniNPU 시뮬레이터 개발 및 성능 분석 리포트

외부 라이브러리(NumPy, Pandas 등) 사용 없이 파이썬 표준 라이브러리만을 활용하여 구축한 **MiniNPU 시뮬레이터**입니다.<br> **NxN** 크기의 패턴과 필터 간 Multiply-Accumulate(MAC) 연산을 수행하고, 패턴 분류 및 연산 성능을 측정·분석합니다.

---

## 1. 실행 방법

### (1) 환경 요건
* **Python 버전:** Python 3.8 이상
* **의존성:** 외부 라이브러리 미사용 (표준 라이브러리 전용)

### (2) 파일 구조
```text
.
├── main.py        # 메인 실행 및 모드 제어 프로그램
├── data.json      # 모드 2 일괄 분석용 데이터셋 (filters 및 patterns 포함)
└── README.md      # 결과 리포트 및 기술 문서

```

### (3) 프로그램 실행

콘솔(터미널)에서 아래 명령어를 실행합니다:

```bash
python main.py

```

### (4) 모드 선택 및 입력 방식

프로그램 실행 후 메뉴 번호(`1`, `2`, `3`)를 입력합니다:

* **`1` (사용자 직접 입력 모드):**
* 행렬 크기 $N$ 입력 후, 필터 A, 필터 B, 패턴 데이터를 공백으로 구분하여 입력합니다.


* **`2` (data.json 자동 일괄 분석 모드):**
* 실행 경로에 위치한 `data.json`을 읽어와 전체 케이스 검증, MAC 판정, 크기별 성능 측정 및 요약 리포트를 자동으로 출력합니다.


* **`3` (프로그램 종료):**
* 프로그램을 안전하게 종료합니다.



---

## 2. 구현 요약

### (1) 라벨 정규화 (Standardization)

`data.json` 또는 사용자 입력 라벨의 다양성(`'+'`, `'cross'`, `'x'`, `'X'`)에 구애받지 않도록, 내부 전처리 함수 `only_normal()`을 통해 표준 라벨(`Cross`, `X`)로 일괄 변환하여 데이터 처리 및 검증의 안정성을 확보했습니다.

### (2) MAC (Multiply-Accumulate) 연산 구현

이중 `for`문 순회 방식으로 직접 구현하였습니다.


$$Score = \sum_{r=0}^{N-1} \sum_{c=0}^{N-1} (Pattern[r][c] \times Filter[r][c])$$


측정 정확도를 높이기 위해 $I/O$ 구간을 제외하고 순수 MAC 연산 구간만을 10회 반복 측정하여 평균 시간(ms)을 산출합니다.

### (3) 부동소수점 비교 및 동점 처리 정책 (Epsilon Policy)

부동소수점 연산 오차로 인한 오판을 방지하기 위해 허용 오차 상수 $\epsilon = 10^{-9}$ 기준을 적용했습니다.

* $\vert{}Score_{Cross} - Score_{X}\vert{} < 10^{-9}$ 인 경우: 동점으로 간주하여 **`UNDECIDED`** 처리
* $Score_{Cross} > Score_{X}$ 인 경우: **`Cross`** 판정
* $Score_{Cross} < Score_{X}$ 인 경우: **`X`** 판정

---

## 3. 결과 리포트

### (1) 테스트 수행 결과 요약
* **총 테스트 케이스:** 6개
* **통과 (PASS):** 3개 (`size_5_2`, `size_13_1`, `size_25_2`)
* **실패 (FAIL):** 3개 (`size_5_1`, `size_13_2`, `size_25_1`)

---

### (2) FAIL 케이스 원인 분석 (데이터/수치 비교 문제)
총 6개의 테스트 케이스 중 3개 케이스에서 `FAIL`이 발생하였으며, 실패 원인 분석 결과는 다음과 같습니다:<br>

* **실패 케이스 목록:** `size_5_1`, `size_13_2`, `size_25_1`
* **주요 원인 (동점 발생으로 인한 `UNDECIDED` 판정):**
  * `size_5_1`: Cross 점수 `0.9` vs X 점수 `0.9` (|`A - B| = 0.0`)
  * `size_13_2`: Cross 점수 `7.5` vs X 점수 `7.5` (`|A - B| = 0.0`)
  * `size_25_1`: Cross 점수 `4.9` vs X 점수 `4.9` (`|A - B| = 0.0`)
* **진단 결과:**
  - 해당 패턴들은 입력 데이터가 Cross 필터와 X 필터 모두에 동일한 유사도를 보이는 대칭 구조를 가지고 있어 MAC 연산 결과가 완벽히 동일했습니다.<br>
  - 프로그램 내부의 허용오차 비교 정책`(|Score_{Cross} - Score_{X}| < 10^{-9})`에 따라 오판을 방지하고 **`UNDECIDED`**로 안전하게 판정하였으나, `expected` 값(`Cross` 또는 `X`)과 불일치하여 `FAIL` 처리되었습니다.<br>

  이는 코드의 로직 오류가 아니며, **부동소수점 Epsilon 정책 및 검증 로직이 의도대로 정밀하게 작동**했음을 입증합니다.<br>

---

### (3) 성능 분석 및 시간 복잡도 O(N^2) 증명

#### [크기별 평균 성능 측정 결과 표]
| 크기 (N x N) | 평균 연산 시간 (ms) | 연산 횟수 (N^2) |
| :---: | :---: | :---: |
| **5 x 5** |    0.0040ms| 25회  |
| **13 x 13** | 0.0311ms | 169회 |
| **25 x 25** | 0.1669ms | 625회 |

* (※ 각 크기별 2개 패턴의 평균 측정 시간을 합산 집계한 결과입니다.)*

#### [시간 복잡도 O(N^2) 근거 서술]
* **원리 분석:** $N \times N$ 크기의 패턴 및 필터 데이터에서 MAC 연산을 수행하기 위해서는 2차원 배열의 모든 원소
($N \times N$)를 1회씩 방문해야 하므로 총 연산 횟수는 정확히 $N^2$번 실행됩니다. 따라서 이론적 시간 복잡도는 **$O(N^2)$**입니다.
* **측정 결과와의 연관성:**
  * $N=5$에서 $N=13$으로 증가 시, 연산 횟수는 $25 \to 169$로 약 **$6.76$배** 증가하였고, 실측 평균 연산 시간 또한 $0.0040\text{ms} \to 0.0311\text{ms}$로 약 **$7.77$배** 증가했습니다.
  * $N=5$에서 $N=25$로 증가 시, 연산 횟수는 $25 \to 625$로 **$25$배** 늘어났고, 실측 평균 연산 시간 역시 $0.0040\text{ms} \to 0.1669\text{ms}$로 약 **$41.7$배** 증가하여 $O(N^2)$ 제곱 비례 관계를 입증하였습니다.

## 4. 트러블슈팅 (Troubleshooting)

### 이슈 1. `self.mode1_run()` 호출 시 `TypeError` 발생

* **원인:** `Mode_1` 클래스 인스턴스 뒤에 소괄호 `()`를 붙여 함수처럼 호출하려고 했으나, 클래스 내부에 `__call__` 메서드가 정의되어 있지 않아 발생함.
* **해결 방법:**
* **방법 A:** `Mode_1` 클래스의 메인 실행 함수명을 `__call__(self)`로 변경하여 인스턴스 직접 호출 가능하게 수정.
* **방법 B:** `self.mode1_run.mode1_flow()` 형태와 같이 명시적인 일반 메서드로 분리 호출하도록 수정.



### 이슈 2. `Matrix` 클래스 인스턴스 생성 시 `TypeError` 발생

* **원인:** 모드 1은 크기만 지정하는 방식(`Matrix(n_size)`), 모드 2는 크기와 데이터를 동시에 전달하는 방식(`Matrix(n_size, data)`)을 사용하여 매개변수 개수 불일치 발생.
* `TypeError: Matrix.__init__() takes 2 positional arguments but 3 were given`
* `TypeError: Matrix.__init__() missing 1 required positional argument: 'data'`


* **해결 방법:** `Matrix` 클래스의 `__init__` 메서드 매개변수에 기본값(`data: list = None`)을 추가하여 1개 및 2개 인자 전달을 모두 유연하게 수용할 수 있도록 개선함.
```python
class Matrix:
    def __init__(self, size: int, data: list = None):
        self.size = size
        self.data = data if data is not None else []

```



---

## 5. 리팩토링 및 모듈화

### `process_pattern_flow` 메서드 구조화

* **기존 문제점:** 단일 메서드 내에서 JSON 파싱, $N$ 크기 추출, 데이터 검증, MAC 연산, 예외 처리까지 모두 담당하여 코드 가독성과 재사용성이 낮고 오점이 발생했을 때 추적이 어려웠음.
* **개선 방향 (단계별 분리):**
* **1단계 (데이터 로드 및 N값 파싱 분리 - `load_data`):** 파일 로드 시점에 키(`size_N`)에서 $N$값을 사전 추출하여 파싱 작업을 전담 처리하도록 분리 완료.
* **2단계 (검증 로직 모듈화):** Cross/X 필터 및 입력 패턴의 데이터 규격 검증 로직을 별도 메서드로 추출하여 가독성과 유지보수성 향상 예정.



---

## 6. 배운 점 (Key Takeaways)

* **불변 데이터와 튜플 (Tuple):** 튜플은 한 번 생성되면 내부 값을 변경할 수 없는 불변(Immutable) 특성을 가지므로, 프로그램 실행 중 변하지 않아야 하는 **고정 데이터 및 설정값 보호**에 최적임을 학습함.
* **`dict.items()` 활용:** 딕셔너리의 키와 값을 동시에 효율적으로 순회하는 방식을 익혀 데이터 파싱 및 요약 리포트 작성 시 코드를 간결하게 작성함.
* **클래스 매개변수 설계:** 클래스 초기화 메서드(`__init__`)에서 디폴트 인자를 활용하는 유연한 인터페이스 설계의 중요성을 체감함.

```

```